# 通过menuId设置手持端按钮是否禁用

## 通过menuId设置手持端按钮是否禁用
## 方法说明
通过menuId设置手持端按钮是否禁用(重启之后会失效，恢复到初始状态)
function setPadDisableByMenuId(menuId: String, disable: boolean):void

## 输入参数
menuId，string 类型，要更改的手持端任务按钮的menuId
disable，boolean 类型，手持端按钮是否设置为禁用
## 输出参数
本方法输出参数为空。
## 异常
本方法不抛出异常。
