# 禁止进入库位进行避让（BinDefect）

### 禁止进入库位进行避让（BinDefect）
如果机器人进入库位避让时会产生风险，可以打开该功能，所有绑定了库位的点位，机器人都不会去避让。

## 参数名称

## 参数位置

## 默认值

## 支持版本

## G-MAPF-BinDefect

## 导航参数

## false

## ~lastest

## 禁止机器人进入库位避让

如果有些库位可以避让，有些不可以，不可以使用这个开关，应该对不能避让的库位单独标记属性为defect，操作可见文档点位属性配置（MAPFProperties）
